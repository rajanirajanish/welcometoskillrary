version: 0.2

phases:
  build:
    commands:
      - echo Entered the build phase...
      - chmod 777 buildspec.yaml
      - mvn clean package
  post_build:
    commands:
      - echo Build completed successfully
artifacts:
  files:
    - webapp/target/webapp.war
  discard-paths: yes
